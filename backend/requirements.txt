# >=3.1.3 for PYSEC-2026-2151.
Flask>=3.1.3
Flask-SQLAlchemy==3.1.1
# >=5.0.0 for CVE-2024-6221 (ACL Private-Network) and related CORS fixes.
Flask-Cors>=5.0.0
Flask-Limiter>=3.8.0
SQLAlchemy==2.0.51
# Schema migrations (SQLite + Postgres). Replaces the old create_all/_migrate.
alembic>=1.18.5
# Postgres driver (optional backend). psycopg 3 bundles libpq via [binary];
# unused when HBOX_DATABASE_URL is blank (SQLite default).
psycopg[binary]>=3.2
PyJWT>=2.10.1
passlib==1.7.4
# passlib 1.7.4 reads bcrypt.__about__ which bcrypt>=4.1 removed; pin below that.
bcrypt==4.0.1
python-dotenv>=1.1.0
qrcode[pil]==7.4.2
# >=23.0.0 for CVE-2024-6827 (Transfer-Encoding request smuggling).
gunicorn>=23.0.0
# MCP server exposed to Home Assistant (runs alongside the app in the same container)
mcp==1.29.0  # pin: newer mcp dropped mcp.server.fastmcp (breaks the MCP server + CI)
uvicorn>=0.52.1
httpx>=0.28.1
# AI providers (lazy-imported by services/ai; only loaded when that provider is
# selected). Ollama/OpenAI-compatible SLMs use httpx / the openai SDK; Claude uses
# the anthropic SDK. A pure-Ollama install never imports these.
# Floors chosen for httpx>=0.28 compatibility: older openai/anthropic pass the
# removed `proxies` kwarg to httpx and crash on client construction.
anthropic>=0.42.0
openai>=1.55.3
